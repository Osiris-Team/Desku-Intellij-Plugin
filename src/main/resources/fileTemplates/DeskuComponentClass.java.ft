#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end

import com.osiris.desku.ui.Component;
import static com.osiris.desku.Statics.*;

public class ${NAME} extends Component<${NAME}> {

    public ${NAME}() {




    }

}